export type MailT = {
    date: string,
    senderName: string,
    senderMail: string,
    receiverName: string,
    receiverMail: string,
    text: string,
    checked: boolean,
    folder: string,
    id: string,
    chosen: boolean
}